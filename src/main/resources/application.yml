server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: none
  data:
    redis:
      host: localhost
      port: 6379
